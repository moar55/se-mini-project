nav(class="navbar navbar-inverse" role="navigation")
    div(class="container-fluid")
      div(class="navbar-header")
        a(class="navbar-brand" href="/") GUC-Portfolio
      ul(class="nav navbar-nav")
        li(class="active")
          a(href="/") Home
      if(session)
        ul(class="nav navbar-nav navbar-right")
          li
            a(href="/students/"+session.id)
              i(class="fa-user" aria-hidden="true") Profile
          li
            a(href="/students/logout")
              i(class="fa-sign-out" aria-hidden="true") Log Out
      -else
        ul(class="nav navbar-nav navbar-right")
          li
            a(href="/students/login")
              i(class="fa-sign-in header" aria-hidden="true") Login
          li
            a(href="/students/register")
              i(class="fa-plus header" aria-hidden="true") &nbsp; Register
